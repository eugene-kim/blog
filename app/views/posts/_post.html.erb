<li id="post_<%= post.id %>">
	<h2><%= post.title %></h2>
	<h3><%= post.created_at %></h3>
	<p><%= post.body %></p>
	<ul>
		<li><%= link_to "Edit", edit_post_path(post) %></li>
		<li><%= link_to "Delete", post, method: 'delete', confirm: "Are you sure?" %>
	</ul>
</li>