<%= form_for task, class: "form-inline", remote: true do |f| %>
  <%= f.check_box :complete %>
  <%= f.submit "Update" %>
  <%= f.label task.name %>
  <%= link_to "✖", task, method: :delete, data: {confirm: "Are you sure?"}, remote: true %>
<% end %>